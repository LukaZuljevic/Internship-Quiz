{
    "name": "internship-quiz",
    "private": true,
    "workspaces": [
        "packages/*",
        "frontend",
        "backend"
    ],
    "scripts": {
        "build": "npm run build --workspaces",
        "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run start:dev --workspace=backend\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
